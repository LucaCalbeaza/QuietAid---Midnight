# Dependencies
**/node_modules/

# Local secrets and environment configuration
**/.env
**/.env.*
!**/.env.example

# macOS
.DS_Store

**/build/
**/dist/

frontend/.env
scholarship-finder-backend/.env

# Local Compact toolchain (downloaded binaries)
.tools/

# Compact compiler output can be large; keep contract source, commit managed for reproducibility of hackathon demo
# midnight-eligibility/contracts/managed/ is committed for demo reliability
